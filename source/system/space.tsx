import React, { useContext } from 'react';

const getTheme = context => {
  const theme = useContext(context);

  // get generators
}


const map = {
  
}

const spaceProps = props => {
  if (props.m) {
    `margin: ${space(props.m) || props.m}`
  }
}

const mapCssPropertiesToProps = props => {

}

const stringifyCssObject = (object) => {

}

// utilities
// map breakpoints to props